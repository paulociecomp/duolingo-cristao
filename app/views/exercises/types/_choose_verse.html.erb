<div data-controller="choose-verse" data-choose-verse-lesson-id-value="<%= exercise.lesson_id %>" data-choose-verse-exercise-id-value="<%= exercise.id %>">
  <div class="mb-8">
    <h2 class="text-xl font-bold text-white mb-6">Qual é o versículo correto?</h2>

    <div class="flex items-start gap-3 mb-6">
      <div class="w-16 h-16 rounded-full bg-[#1A2C32] flex items-center justify-center text-3xl flex-shrink-0">
        📖
      </div>
      <div class="bg-[#1A2C32] rounded-2xl rounded-tl-none px-4 py-3 flex-1">
        <p class="text-white text-lg italic">"<%= exercise.content["verse_text"] %>"</p>
      </div>
    </div>

    <p class="text-gray-400 text-sm">Escolha a referência correta:</p>
  </div>

  <div class="space-y-3" data-choose-verse-target="options">
    <% exercise.content["options"].each do |option| %>
      <button type="button"
              class="w-full text-left p-4 bg-[#1A2C32] border-2 border-[#37464F] rounded-2xl text-white hover:border-[#4A5D66] transition-all duration-200 focus:outline-none"
              data-action="click->choose-verse#select"
              data-choose-verse-target="option"
              data-value="<%= option %>">
        <div class="flex items-center gap-3">
          <span class="text-xl">📖</span>
          <span class="font-medium"><%= option %></span>
        </div>
      </button>
    <% end %>
  </div>

  <input type="hidden" name="answer" data-choose-verse-target="answer" data-exercise-target="answer">
</div>
