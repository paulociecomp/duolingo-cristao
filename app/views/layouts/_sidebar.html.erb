<aside class="hidden lg:flex lg:flex-col lg:w-64 lg:fixed lg:inset-y-0 lg:pt-16 bg-[#1A2C32] border-r border-[#37464F]">
  <div class="flex-1 flex flex-col pt-5 pb-4 overflow-y-auto">
    <nav class="flex-1 px-4 space-y-1">
      <%= sidebar_link_to "Trilhas", root_path, icon: "📚" %>
      <%= sidebar_link_to "Ranking", leaderboard_index_path, icon: "🏅" %>
      <%= sidebar_link_to "Missões", missions_path, icon: "⭐" %>
      <%= sidebar_link_to "Conquistas", achievements_path, icon: "🏆" %>
      <%= sidebar_link_to "Revisão", reviews_path, icon: "🔄" %>
      <%= sidebar_link_to "Loja", shop_index_path, icon: "🛒" %>
    </nav>
  </div>

  <div class="flex-shrink-0 p-4 border-t border-[#37464F]">
    <div class="bg-gradient-to-r from-[#58CC02] to-[#1CB0F6] rounded-xl p-4 text-white">
      <div class="text-sm font-medium mb-1">Nível <%= current_user.current_level %></div>
      <div class="w-full bg-white/30 rounded-full h-2 mb-2">
        <div class="bg-white rounded-full h-2 transition-all duration-300" style="width: <%= LevelService.new(current_user).progress_percentage %>%"></div>
      </div>
      <div class="text-xs opacity-80">
        <%= LevelService.new(current_user).xp_needed_for_next_level %> XP para o próximo nível
      </div>
    </div>
  </div>
</aside>
