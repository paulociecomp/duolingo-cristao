<div class="max-w-lg mx-auto">
  <div class="mb-8">
    <h1 class="text-3xl font-bold text-gray-900 mb-2">Missões Diárias</h1>
    <p class="text-gray-600">Complete missões para ganhar XP e Gems extras</p>
  </div>

  <div class="space-y-4">
    <% @missions.each do |user_mission| %>
      <% mission = user_mission.daily_mission %>
      <div class="bg-white rounded-xl border border-gray-200 p-4 <%= user_mission.completed? ? 'bg-green-50 border-green-200' : '' %>">
        <div class="flex items-center gap-4">
          <div class="text-3xl"><%= mission.icon %></div>

          <div class="flex-1">
            <div class="font-semibold text-gray-900"><%= mission.name %></div>
            <div class="text-sm text-gray-500"><%= mission.description %></div>

            <div class="mt-2">
              <div class="flex items-center justify-between text-sm mb-1">
                <span class="text-gray-500"><%= user_mission.current_value %> / <%= mission.target_value %></span>
                <span class="text-gray-500"><%= user_mission.progress_percentage %>%</span>
              </div>
              <div class="w-full bg-gray-200 rounded-full h-2">
                <div class="h-2 rounded-full transition-all <%= user_mission.completed? ? 'bg-green-500' : 'bg-indigo-500' %>" style="width: <%= user_mission.progress_percentage %>%"></div>
              </div>
            </div>
          </div>

          <div class="text-right">
            <% if user_mission.completed? && !user_mission.claimed? %>
              <%= button_to claim_mission_path(user_mission), method: :post, class: "bg-green-500 hover:bg-green-600 text-white font-semibold px-4 py-2 rounded-lg transition" do %>
                Coletar
              <% end %>
            <% elsif user_mission.claimed? %>
              <div class="text-green-500 font-semibold">✓ Coletado</div>
            <% else %>
              <div class="text-sm text-gray-400">
                <div class="flex items-center gap-1 text-amber-500">
                  <span>⭐</span>
                  <span>+<%= mission.xp_reward %></span>
                </div>
                <% if mission.gem_reward > 0 %>
                  <div class="flex items-center gap-1 text-cyan-500">
                    <span>💎</span>
                    <span>+<%= mission.gem_reward %></span>
                  </div>
                <% end %>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    <% end %>
  </div>

  <% if @missions.empty? %>
    <div class="text-center py-12">
      <div class="text-6xl mb-4">⭐</div>
      <h2 class="text-xl font-semibold text-gray-900 mb-2">Missões carregando...</h2>
      <p class="text-gray-600">As missões diárias serão geradas quando você começar a estudar.</p>
    </div>
  <% end %>
</div>
