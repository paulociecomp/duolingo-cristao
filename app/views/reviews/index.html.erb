<div class="max-w-lg mx-auto">
  <div class="mb-8">
    <h1 class="text-3xl font-bold text-gray-900 mb-2">Revisão Espaçada</h1>
    <p class="text-gray-600">Revise exercícios para fixar o conhecimento</p>
  </div>

  <!-- Stats -->
  <div class="grid grid-cols-2 gap-4 mb-8">
    <div class="bg-white rounded-xl p-4 border border-gray-200 text-center">
      <div class="text-3xl font-bold text-indigo-600"><%= @stats[:due_today] %></div>
      <div class="text-sm text-gray-500">Para revisar hoje</div>
    </div>
    <div class="bg-white rounded-xl p-4 border border-gray-200 text-center">
      <div class="text-3xl font-bold text-green-600"><%= @stats[:mastered] %></div>
      <div class="text-sm text-gray-500">Dominados</div>
    </div>
  </div>

  <% if @cards_due.any? %>
    <%= link_to review_path(@cards_due.first), class: "block w-full bg-indigo-600 hover:bg-indigo-700 text-white font-semibold py-4 px-6 rounded-xl transition text-center mb-6" do %>
      <div class="flex items-center justify-center gap-2">
        <span class="text-xl">🔄</span>
        <span>Iniciar Revisão (<%= @cards_due.size %> cards)</span>
      </div>
    <% end %>

    <div class="space-y-3">
      <% @cards_due.limit(5).each do |card| %>
        <div class="bg-white rounded-lg border border-gray-200 p-4">
          <div class="flex items-center justify-between">
            <div>
              <div class="font-medium text-gray-900"><%= card.exercise.lesson.name %></div>
              <div class="text-sm text-gray-500"><%= card.exercise.exercise_type.humanize %></div>
            </div>
            <div class="text-sm text-gray-400">
              Intervalo: <%= card.interval_days %>d
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="text-center py-12">
      <div class="text-6xl mb-4">✅</div>
      <h2 class="text-xl font-semibold text-gray-900 mb-2">Tudo revisado!</h2>
      <p class="text-gray-600">Não há cards para revisar agora. Continue estudando para adicionar mais!</p>
    </div>
  <% end %>
</div>
